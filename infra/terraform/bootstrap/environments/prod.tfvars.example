aws_region  = "us-east-1"
project     = "platform-ops"
environment = "prod"

# Optional fixed bucket name. Leave empty to auto-generate unique suffix.
# state_bucket_name = "platform-ops-prod-tfstate-yourcompany"
