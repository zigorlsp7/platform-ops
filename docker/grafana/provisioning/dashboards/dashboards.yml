apiVersion: 1

providers:
  - name: "cv-observability"
    folder: "CV Observability"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards