#!/usr/bin/env sh
set -eu

branch="$(git rev-parse --abbrev-ref HEAD)"
if [ "$branch" = "main" ]; then
  echo "Direct commits to 'main' are blocked. Create a feature branch and open a PR." >&2
  exit 1
fi

npm run check:hooks
